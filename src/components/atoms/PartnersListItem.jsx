// @flow

import styled from 'styled-components';

const PartnersListItem = styled.div`
  max-width: 100%;
  position: relative;
  margin-bottom: 0.75rem;
  .media: not(:last-child);
`;

export default PartnersListItem;
